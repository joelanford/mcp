# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Source Control

Check whether the local repo uses `jj` (Jujutsu) or `git` before running SCM commands. Look for `.jj/` or `.git/` directories to determine which tool to use.
